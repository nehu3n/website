---
import Header from '../components/Header.astro';
import Project from "../components/Project.astro";
import Layout from '../layouts/Layout.astro';
import Footer from '../components/Footer.astro';
import Post from '../components/Post.astro';

import { PROJECTS } from "../consts" ;
import { getCollection } from 'astro:content';

const posts = (await getCollection('blog')).sort(
	(a, b) => b.data.pubDate.valueOf() - a.data.pubDate.valueOf(),
);
---

<Layout title="Nehuén's portfolio">
  <main class="mx-auto max-w-content px-6 pt-24">
    <Header />

    <section class="mb-16">
      <h1 class="text-4xl font-medium tracking-tight text-zinc-900 dark:text-zinc-100">
        Hi, I'm <span class="text-zinc-700 dark:text-zinc-300">Nehuén</span> 🐈
      </h1>
      <p class="mt-6 text-lg leading-relaxed text-zinc-600 dark:text-zinc-400">
        I am a young and passionate developer, focused on developing tools and technologies that redefine the current paradigm.
      </p>
    </section>

    <section id="projects" class="mb-16">
      <h2 class="text-2xl font-medium text-zinc-900 dark:text-zinc-100 mb-8">Selected Projects</h2>
      <div class="grid gap-6">
        {PROJECTS.map(project => (
          <Project {...project} />
        ))}
      </div>
    </section>

        <section id="about" class="mb-16">
      <h2 class="text-2xl font-medium text-zinc-900 dark:text-zinc-100 mb-6">About</h2>
      <p class="text-zinc-600 dark:text-zinc-400 leading-relaxed">
        I've always had a computer, so since I was ten years old I've been using it extensively. I started to approach programming when I was eleven, watching videos and reading about languages and technologies such as <span class="text-zinc-700 dark:text-zinc-300">Godot</span> or <span class="text-zinc-700 dark:text-zinc-300">JavaScript</span>. My first “projects” were within the social network Discord, where to practice and continue gaining experience I developed bots for a few years.
        <br/>
        <br/>
        Then I discovered other languages like <span class="text-zinc-700 dark:text-zinc-300">Go</span>, <span class="text-zinc-700 dark:text-zinc-300">TypeScript</span>, <span class="text-zinc-700 dark:text-zinc-300">Python</span> and, a little more than a year ago, <span class="text-zinc-700 dark:text-zinc-300">Rust</span>. Also around the age of thirteen I got into web development. I always liked to improve and increase my knowledge, studying a variety of content, from the most theoretical to its practical application. I create software that is useful to me and the influenced ecosystem. I am very perfectionist and defender of my position in the opinions I give, although I have no problem to accept if I'm wrong.
      </p>
    </section>

    <section id="posts" class="mb-16">
      <h2 class="text-2xl font-medium text-zinc-900 dark:text-zinc-100 mb-8">Recent Posts</h2>
      <ul class="grid gap-8 md:grid-cols-1 lg:grid-cols-2 xl:grid-cols-2">
        {posts.slice(0, 3).map((post) => (
          <Post slug={post.slug} title={post.data.title} description={post.data.description} pubDate={post.data.pubDate} heroImage={post.data.heroImage} updatedDate={post.data.updatedDate} />
        ))}
      </ul>
    </section>

    <Footer />
</Layout>