---
const techs = [
	"rust",
	"go",
	"elixir",
	"typescript",
	"c",
	"compilers",
	"p2p",
	"backend",
];
---

<section class="min-h-screen flex flex-col items-center justify-start pt-12 md:pt-20 px-4 animate-fade-in relative overflow-hidden">
  <div class="text-center w-full max-w-4xl mx-auto px-4 md:px-8 relative z-10">
    <div class="-mb-20 md:-mb-32 flex justify-center relative z-0">
      <div class="w-64 h-64 md:w-96 md:h-96 flex items-end justify-center">
        <img 
          src="/cat-line.webp" 
          alt="cat" 
          class="w-full h-auto opacity-90 invert brightness-200 pointer-events-none select-none"
        />
      </div>
    </div>

    <h1 class="text-6xl md:text-9xl font-extralight tracking-tighter mb-8 text-zinc-100 relative z-10 leading-none">
      nehuén
    </h1>

    <p class="text-lg md:text-xl text-zinc-500 font-light mb-16 tracking-[0.2em] uppercase">
      building fast, quiet systems
    </p>

    <div class="flex flex-wrap justify-center gap-4 animate-slide-up">
      {techs.map((tech) => (
        <span class="px-6 py-2 bg-transparent text-zinc-500 font-mono text-xs border border-zinc-900 hover:border-zinc-700 hover:text-zinc-200 transition-all duration-500 uppercase tracking-widest">
          {tech}
        </span>
      ))}
    </div>
  </div>

  <div class="absolute bottom-12 left-1/2 -translate-x-1/2 animate-bounce text-zinc-500 text-2xl font-light opacity-50">
    ↓
  </div>  
</section>

<style>
  section {
    perspective: 1000px;
  }
</style>
